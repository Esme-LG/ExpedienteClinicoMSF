@model IEnumerable<ExpedienteClinicoMSF.Models.Regiones>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Region)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegRegion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Region)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RegRegion.Region)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RegionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RegionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RegionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
