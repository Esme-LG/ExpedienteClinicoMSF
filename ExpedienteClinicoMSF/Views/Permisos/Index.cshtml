@model IEnumerable<ExpedienteClinicoMSF.Models.Permisos>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Permiso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoPermiso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Permiso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoPermiso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PermisoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PermisoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PermisoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
