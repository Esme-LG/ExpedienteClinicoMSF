@model IEnumerable<ExpedienteClinicoMSF.Models.Generos>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Genero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Terminacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Genero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Terminacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GeneroId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GeneroId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GeneroId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
