@model IEnumerable<ExpedienteClinicoMSF.Models.EstadosCiviles>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCivil)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoCivil)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstadoCivilId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstadoCivilId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstadoCivilId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
